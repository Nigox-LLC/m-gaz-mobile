/// The three gas-network measuring-device document lists. Each value maps to a
/// distinct backend endpoint in the data layer and to a screen title in the
/// presentation layer. Kept free of any transport/UI concern (pure domain).
enum MeasurementDeviceType { gts, industrialCollectors, technological }
